# Thrustmaster Controller

A Linux prototype for discovering a Thrustmaster USB controller and visualizing
its live buttons, axes, and hat switches.

Development work is based on the `develop` branch. The first hardware bring-up
lives on `feature/bringup`.

